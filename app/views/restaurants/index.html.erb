<div class="my-container">
  <h1>Restaurants</h1>
  <% @restaurants.each do |restaurant| %>
    <div  class ="inner">
    <h3> <%= restaurant.name %></h3>
    <%= link_to "See details", restaurant_path(restaurant), class: "btn btn-primary ms-3"%>
    </div>
  <% end %>
  <%= link_to 'create restaurant', new_restaurant_path, class: "index-btn btn btn-primary w-20" %>
</div>
